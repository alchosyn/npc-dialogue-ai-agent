# Training dependencies for SFT / LoRA fine-tuning of Qwen2.5-1.5B
# Install in a Kaggle notebook or Colab Pro GPU runtime.

# Base training stack（版本范围按 2024 末 - 2025 兼容性测试过）
torch>=2.1.0
transformers>=4.40.0,<5.0.0
peft>=0.10.0
trl>=0.8.0,<1.0.0
datasets>=2.18.0
accelerate>=0.28.0

# 8-bit AdamW 优化器（adamw_8bit）依赖此包，省显存
# Kaggle T4 上有时需要 bitsandbytes-cuda118 / cuda121 匹配版本
bitsandbytes>=0.43.0

# Optional acceleration on consumer GPUs (T4/P100/A100):
# Unsloth 现在官方推荐直接 pip install unsloth，自动选 CUDA wheel：
#   pip install unsloth
# 详细见 https://github.com/unslothai/unsloth#installation-instructions

# GRPO 专用：trl 的 GRPOTrainer import 时连锁 mergekit（这版 trl 没软导入）。
# Kaggle 不预装，缺它会 RuntimeError: Failed to import trl.trainer.grpo_trainer。
# 装时建议 --no-deps，避免改动已装好的 torch/transformers。
mergekit

# Eval helpers
scipy>=1.10.0
